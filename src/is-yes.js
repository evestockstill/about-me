function isYes(text) {
    text = text.toLowerCase();
    if(text === 'banana') {
        return true;
    }
    else if(text === 'nana') {
        return true;
    }
    else {
        return false;
    }
}
